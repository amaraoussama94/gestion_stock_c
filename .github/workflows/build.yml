# Defines a GitHub Actions workflow named "C Build and Release"
name: C Build and Release

on:
  push:
    branches:
      - master  # This workflow runs when code is pushed to the master branch.

jobs:
  build:
    name: Build and Release
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.new_version }}  # ✅ Expose version to downstream jobs

    steps:
    # Downloads your code from GitHub into the runner so all files are accessible.
    - name: Checkout repository
      uses: actions/checkout@v3
      # Fix the ref to master branch and fetch all history for gcovr
      with:
        ref: master
        fetch-depth: 0

    # Installs all the tools you need (compiler, make, etc.) on the Ubuntu runner. 
    # This is your build environment setup.
    - name: Install build tools
      run: |
        sudo apt-get update && sudo apt-get install -y build-essential make

    # Installs the MinGW cross-compiler, which allows you to compile C code for Windows on a Linux machine.
    - name: Install MinGW cross-compiler
      run: sudo apt-get install -y mingw-w64

    # Reads the current version from version.txt and bumps it by 0.01.
    - name: Read and bump version
      id: version
      run: |
        VERSION=$(cat version.txt | tr -d 'v')
        MAJOR=$(echo "$VERSION" | cut -d. -f1)
        MINOR=$(echo "$VERSION" | cut -d. -f2)
        NEW_MINOR=$(printf "%02d" $((10#$MINOR + 1)))
        NEW_VERSION="v$MAJOR.$NEW_MINOR"
        echo "$NEW_VERSION" > version.txt
        echo "new_version=$NEW_VERSION" >> $GITHUB_OUTPUT

    # Build Linux binary
    - name: Build Linux binary
      run: |
        VERSION=$(cat version.txt | tr -d 'v')
        make ci-build VERSION=$VERSION

    # Build Windows binary
    - name: Build Windows binary
      run: |
        VERSION=$(cat version.txt | tr -d 'v')
        make ci-build-windows VERSION=$VERSION

    # Verifies binaries were successfully created
    - name: Check binaries exist
      run: |
        VERSION=${{ steps.version.outputs.new_version }}
        [ -f "build/gestion_stock_${VERSION}" ] || { echo "Linux binary missing"; exit 1; }
        [ -f "build/gestion_stock_${VERSION}.exe" ] || { echo "Windows binary missing"; exit 1; }

    # Uploads binaries to GitHub Release using the created tag
    - name: Upload binaries to GitHub Release
      uses: softprops/action-gh-release@v2
      with:
        tag_name: PROD_VERSION_${{ steps.version.outputs.new_version }}
        files: |
          build/gestion_stock_${{ steps.version.outputs.new_version }}
          build/gestion_stock_${{ steps.version.outputs.new_version }}.exe
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    # Saves binaries as CI artifacts for use in the next job
    - name: Upload binaries as artifacts
      uses: actions/upload-artifact@v4
      with:
        name: synced-binaries
        path: |
          build/gestion_stock_${{ steps.version.outputs.new_version }}
          build/gestion_stock_${{ steps.version.outputs.new_version }}.exe

  push-to-test-repo:
    name: Push Binaries to Test Repo
    needs: build  # Depends on completion of build job
    runs-on: ubuntu-latest

    steps:
      # Downloads the artifacts created in the build job
      - name: Download binaries from build job
        uses: actions/download-artifact@v4
        with:
          name: synced-binaries

      # Clones the test repository using personal access token
      - name: Clone Test Repo
        run: |
          git clone https://x-access-token:${{ secrets.TEST_REPO_PUSH_TOKEN }}@github.com/amaraoussama94/Test_Auto_gestion_stock_c.git test_repo

      # Copies binaries into build/ folder in test repo
      - name: Copy binaries into test repo
        run: |
          VERSION="${{ needs.build.outputs.version }}"
          echo "Using version: $VERSION"
          mkdir -p test_repo/build/
          cp gestion_stock_${VERSION} test_repo/build/gestion_stock_linux
          cp gestion_stock_${VERSION}.exe test_repo/build/gestion_stock.exe

      # Commits and pushes binaries to the test repository
      - name: Commit and push to test repo
        run: |
          cd test_repo
          git config user.name "CI Binary Sync"
          git config user.email "ci@binary-sync.com"
          git add build/
          git commit -m "🔄 Sync binaries from gestion_stock_c [${{ needs.build.outputs.version }}]" || echo "No changes to commit"
          # 🛡️ Force Git to use token authentication for push
          git remote set-url origin https://x-access-token:${{ secrets.TEST_REPO_PUSH_TOKEN }}@github.com/amaraoussama94/Test_Auto_gestion_stock_c.git
          git push origin master --force

